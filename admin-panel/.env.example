# Copy to .env for local dev (optional — leave blank to use the Vite proxy).
# In production set this to the deployed backend URL, e.g.
#   VITE_API_URL=https://gym-production-e3fb.up.railway.app
VITE_API_URL=
